################### Jolokiabeat Configuration Example #########################

############################# Jolokiabeat ######################################

jolokiabeat:
  # Defines how often an event is sent to the output
  period: 10s
  metric_under_root: false
  metric_field_name: jolokia
  config_dir: jolokiabeat.d
